let idade = 20;

let mensagem = idade >= 18 ? "Maior de idade" : "Menor de idade";

console.log(mensagem);

/*
Exercício: Utilizando o operador ternário, crie um script que verifique
se um aluno está aprovado ou reprovado
*/

const nota = 15;
const notaMinima = 10;

const resultado = nota >= notaMinima ? "Aprovado" : "Reprovado";

console.log(`O aluno está: ${resultado}`);
